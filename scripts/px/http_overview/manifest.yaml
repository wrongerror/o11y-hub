---
short: HTTP Overview
long: >
  Monitor HTTP request metrics including latency, error rate, and throughput.
  Provides comprehensive HTTP performance analysis across services.

category: Application
tags:
  - http
  - performance
  - sla

parameters:
  - name: start_time
    type: string
    description: "Start time for the query (e.g., -5m, -1h)"
    default: "-5m"
    required: true
  - name: namespace
    type: string
    description: "Kubernetes namespace to filter (empty for all)"
    default: ""
    required: false
  - name: service
    type: string
    description: "Service name to filter (empty for all)"
    default: ""
    required: false
