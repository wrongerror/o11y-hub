# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
observo-connector
mock-server

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Local certificates (keep structure but ignore actual certs)
certs/*.crt
certs/*.key
certs/*.pem
certs/*.p12
certs/*.pfx
!certs/.gitkeep

# Logs
*.log

# Environment files
.env
.env.local
.env.*.local

# Generated protobuf files (uncomment if you want to regenerate them each time)
# proto/*.pb.go

# Build artifacts
dist/
build/

# Local configuration
config.yaml
config.json
*.local.*
